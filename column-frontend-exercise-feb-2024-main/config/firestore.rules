rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all documents
    match /{document=**} {
      allow read;
    }
    
    // Allow write access to the 'notices' collection
    match /notices/{noticeId} {
      allow create: if true;
    }
  }
}
